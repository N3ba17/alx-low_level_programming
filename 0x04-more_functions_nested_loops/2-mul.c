#include "main.h"
/**
 * mul - check if upper case
 * @a : first integer
 * @b : second integer
 * Return: int 1 if true and  0 if false
 * Description - check if c is upper case
 */
int mul(int a, int b)
{
	return (a * b);
}
